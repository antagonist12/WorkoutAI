import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  content: {
    paddingVertical: 12,
  },
  loadingContainer: {
    padding: 12,
    alignItems: 'flex-start',
    marginLeft: 12,
  },
});
